# Local dev: leave empty — Vite proxies /api to http://localhost:3001 (see vite.config.js)
# Docker / Render with SERVE_CLIENT=true: leave empty (same origin)
# Separate API host only: VITE_API_URL=https://your-api.example.com
